<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OpenClawTray.Pages.ConnectionPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Padding="24" Spacing="16" HorizontalAlignment="Stretch">

            <TextBlock Text="🔗 Connection" Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock Text="Gateway endpoint used by both the operator client and node service."
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,0,0,8"/>

            <!-- ═══ Section 1: Status Card ═══ -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="16">
                <StackPanel Spacing="8">
                    <!-- Row 1: Status + Reconnect -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8">
                            <Ellipse x:Name="StatusDot" Width="12" Height="12" VerticalAlignment="Center" Fill="Gray"/>
                            <TextBlock x:Name="StatusText" Text="Disconnected" VerticalAlignment="Center"
                                       Style="{StaticResource SubtitleTextBlockStyle}"/>
                        </StackPanel>
                        <Button x:Name="ReconnectButton" Grid.Column="1" Content="Reconnect"
                                VerticalAlignment="Center" Click="OnReconnect" IsEnabled="False"/>
                    </Grid>
                    <!-- Row 2: Gateway details -->
                    <TextBlock x:Name="GatewayDetailText"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               TextWrapping="Wrap"/>
                    <!-- Row 3: URL + auth mode -->
                    <TextBlock x:Name="GatewayUrlDetail"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               TextWrapping="Wrap"/>
                    <!-- Row 4: Dual client status -->
                    <StackPanel Orientation="Horizontal" Spacing="16">
                        <TextBlock x:Name="OperatorStatusText" Text="Operator: —"
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <TextBlock x:Name="NodeStatusText" Text="Node: —"
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                    <!-- Row 5: Connect/Disconnect toggle -->
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <ToggleSwitch x:Name="ConnectToggle" Header="Connection"
                                      Toggled="OnConnectToggled"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- ═══ Section 2: Gateway Discovery ═══ -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="16">
                <StackPanel Spacing="12">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="📡 Available Gateways"
                                   Style="{StaticResource BodyStrongTextBlockStyle}" VerticalAlignment="Center"/>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                            <ProgressRing x:Name="ScanProgressRing" IsActive="False" Width="16" Height="16"
                                          VerticalAlignment="Center" Visibility="Collapsed"/>
                            <Button x:Name="ScanButton" Click="OnScanForGateways" VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal" Spacing="6">
                                    <FontIcon Glyph="&#xE11A;" FontSize="14"/>
                                    <TextBlock Text="Scan"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>
                    <StackPanel x:Name="GatewayListPanel" Spacing="4" Visibility="Collapsed"/>
                    <TextBlock x:Name="GatewayEmptyText" Text="No gateways found. Click Scan to search."
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
            </Border>

            <!-- ═══ Section 3: Setup Code ═══ -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="16">
                <StackPanel Spacing="8">
                    <TextBlock Text="🔑 Setup Code" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                    <TextBlock Text="Paste a setup code from openclaw qr"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="SetupCodeTextBox" Grid.Column="0"
                                 PlaceholderText="Paste setup code here…"/>
                        <Button x:Name="ApplySetupCodeButton" Grid.Column="1" Content="Apply"
                                Style="{ThemeResource AccentButtonStyle}"
                                Click="OnApplySetupCode" VerticalAlignment="Bottom"/>
                    </Grid>
                    <TextBlock x:Name="SetupCodeResultText"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
            </Border>

            <!-- ═══ Section 4: Advanced Connection Settings ═══ -->
            <Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" IsExpanded="True">
                <Expander.Header>
                    <TextBlock Text="Advanced Connection Settings" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                </Expander.Header>
                <StackPanel Spacing="12" Padding="4">
                    <TextBox x:Name="GatewayUrlTextBox" Header="Gateway URL"
                             PlaceholderText="ws://localhost:18789 or https://host.tailnet.ts.net"/>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <TextBox x:Name="TokenTextBox" Header="Token"
                                 PlaceholderText="Your API token" Width="350"/>
                        <Button x:Name="TestButton" Content="Test" VerticalAlignment="Bottom"
                                Click="OnTestConnection"/>
                    </StackPanel>
                    <TextBlock x:Name="TestResultText"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>

                    <!-- SSH Tunnel -->
                    <ToggleSwitch x:Name="SshToggle" Header="SSH Tunnel"
                                  Toggled="OnSshToggled" Margin="0,4,0,0"/>
                    <StackPanel x:Name="SshDetailsPanel" Spacing="8" Visibility="Collapsed">
                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="SshUserBox" Grid.Column="0" Header="SSH User" PlaceholderText="user"/>
                            <TextBox x:Name="SshHostBox" Grid.Column="1" Header="SSH Host" PlaceholderText="machine-name"/>
                        </Grid>
                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="SshRemotePortBox" Grid.Column="0" Header="Remote Port" PlaceholderText="18789"/>
                            <TextBox x:Name="SshLocalPortBox" Grid.Column="1" Header="Local Port" PlaceholderText="18789"/>
                        </Grid>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                        <Button Content="Save" Style="{ThemeResource AccentButtonStyle}" Click="OnSave"/>
                    </StackPanel>
                </StackPanel>
            </Expander>

            <!-- ═══ Section 5: Device Identity ═══ -->
            <Border x:Name="DeviceIdentityCard" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="16" Visibility="Collapsed">
                <StackPanel Spacing="8">
                    <TextBlock Text="🪪 This Device" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="Device ID:" VerticalAlignment="Center"
                                   Style="{StaticResource CaptionTextBlockStyle}"/>
                        <TextBlock x:Name="DeviceIdText" Grid.Column="1" VerticalAlignment="Center"
                                   Style="{StaticResource BodyTextBlockStyle}" IsTextSelectionEnabled="True"/>
                        <Button x:Name="CopyDeviceIdButton" Grid.Column="2" Content="Copy"
                                Click="OnCopyDeviceId" VerticalAlignment="Center"/>
                    </Grid>
                    <TextBlock x:Name="PairingStatusText"
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <!-- Pending approval help -->
                    <StackPanel x:Name="ApprovalHelpPanel" Spacing="4" Visibility="Collapsed">
                        <TextBlock Text="Run this command on the gateway host to approve:"
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <Grid ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock x:Name="ApprovalCommandText" Grid.Column="0"
                                       IsTextSelectionEnabled="True"
                                       Style="{StaticResource CaptionTextBlockStyle}"
                                       FontFamily="Consolas" TextWrapping="Wrap"/>
                            <Button x:Name="CopyApprovalButton" Grid.Column="1" Content="Copy"
                                    Click="OnCopyApprovalCommand" VerticalAlignment="Top"/>
                        </Grid>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- ═══ Section 6: Connection Log ═══ -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="16">
                <StackPanel Spacing="8">
                    <TextBlock Text="📋 Connection Log" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                    <StackPanel x:Name="ConnectionLogPanel" Spacing="2"/>
                    <TextBlock x:Name="ConnectionLogEmpty" Text="No recent connection events."
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
