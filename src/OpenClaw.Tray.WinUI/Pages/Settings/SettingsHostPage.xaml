<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OpenClawTray.Pages.Settings.SettingsHostPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <NavigationView x:Name="TreeNav"
                    PaneDisplayMode="Left"
                    IsPaneToggleButtonVisible="False"
                    IsBackButtonVisible="Collapsed"
                    IsSettingsVisible="False"
                    OpenPaneLength="260"
                    SelectionChanged="OnTreeSelectionChanged">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Variant C-2: Back button to return Hub to full-screen Chat. -->
            <Button x:Name="BackToHomeButton"
                    Grid.Row="0"
                    Margin="16,12,16,0"
                    Padding="10,4,12,4"
                    HorizontalAlignment="Left"
                    Background="Transparent" BorderThickness="0"
                    Click="OnBackToHomeClick"
                    AutomationProperties.AutomationId="SettingsHost_Back">
                <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE72B;" FontSize="14" VerticalAlignment="Center"/>
                    <TextBlock Text="Back" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>

            <!-- Breadcrumb / context line -->
            <Border Grid.Row="1"
                    Padding="24,12,24,12"
                    BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                    BorderThickness="0,0,0,1">
                <StackPanel Spacing="2">
                    <TextBlock x:Name="BreadcrumbText"
                               Text="Settings"
                               Style="{StaticResource SubtitleTextBlockStyle}"
                               TextTrimming="CharacterEllipsis"/>
                    <TextBlock x:Name="BreadcrumbHint"
                               Text=""
                               Style="{StaticResource CaptionTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               TextWrapping="NoWrap"
                               TextTrimming="CharacterEllipsis"/>
                </StackPanel>
            </Border>

            <!-- Right pane content host -->
            <Grid Grid.Row="2">
                <ScrollViewer x:Name="RootHost"
                              VerticalScrollBarVisibility="Auto"
                              Padding="24,16,24,24"/>
                <Frame x:Name="SubFrame" Visibility="Collapsed"/>
            </Grid>
        </Grid>
    </NavigationView>
</Page>
