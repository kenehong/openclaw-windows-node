<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="OpenClawTray.Pages.CronPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Padding="24" Spacing="16" HorizontalAlignment="Stretch">

            <TextBlock Text="⏱️ Cron Jobs" Style="{StaticResource TitleTextBlockStyle}"/>

            <!-- Not-wired InfoBar -->
            <InfoBar x:Name="NotWiredInfoBar"
                     IsOpen="True" IsClosable="False"
                     Severity="Warning"
                     Title="Cron API not yet wired"
                     Message="Showing sample data. Cron management methods are not yet available in OpenClawGatewayClient."/>

            <!-- Scheduler Status Card -->
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Spacing="4">
                        <TextBlock Text="Scheduler Status" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Ellipse x:Name="SchedulerStatusIndicator" Width="10" Height="10"
                                     Fill="LimeGreen" VerticalAlignment="Center"/>
                            <TextBlock x:Name="SchedulerStatusText" Text="Enabled"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock x:Name="StorePathText" Text="Store: ~/.openclaw/cron.db"
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                        <TextBlock x:Name="NextWakeText" Text="Next wake: —"
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    </StackPanel>
                    <ToggleSwitch x:Name="SchedulerToggle" Grid.Column="1"
                                  IsOn="True" VerticalAlignment="Center"
                                  Toggled="OnSchedulerToggled"/>
                </Grid>
            </Border>

            <!-- Jobs List -->
            <ListView x:Name="JobsList" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,4">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Row 0: Name + Schedule -->
                                <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal" Spacing="8">
                                    <TextBlock Text="{Binding Name}" FontWeight="Bold" VerticalAlignment="Center"/>
                                    <Border Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                            CornerRadius="4" Padding="6,2">
                                        <TextBlock Text="{Binding Schedule}" FontFamily="Consolas"
                                                   Style="{StaticResource CaptionTextBlockStyle}"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    </Border>
                                </StackPanel>

                                <!-- Row 0: Action buttons -->
                                <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" Spacing="4">
                                    <Button Content="Run Now" Tag="{Binding Id}" Click="OnRunNowClick"/>
                                    <ToggleSwitch IsOn="{Binding IsEnabled}" MinWidth="0"
                                                  OnContent="" OffContent=""/>
                                    <Button Content="✕" Tag="{Binding Id}" Click="OnRemoveClick"
                                            ToolTipService.ToolTip="Remove job"/>
                                </StackPanel>

                                <!-- Row 1: Last run -->
                                <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="8"
                                            Margin="0,4,0,0">
                                    <TextBlock Text="Last run:" Style="{StaticResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    <TextBlock Text="{Binding LastRunTime}" Style="{StaticResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    <Border CornerRadius="4" Padding="6,1"
                                            Background="{Binding ResultBadgeBackground}">
                                        <TextBlock Text="{Binding LastResult}" FontSize="11"
                                                   Foreground="White"/>
                                    </Border>
                                </StackPanel>

                                <!-- Row 2: Next run -->
                                <StackPanel Grid.Row="2" Grid.ColumnSpan="2" Orientation="Horizontal" Spacing="8"
                                            Margin="0,2,0,0">
                                    <TextBlock Text="Next run:" Style="{StaticResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                    <TextBlock Text="{Binding NextRunTime}" Style="{StaticResource CaptionTextBlockStyle}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <!-- Empty State -->
            <StackPanel x:Name="EmptyState" Visibility="Collapsed"
                        VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="8"
                        Margin="0,32,0,0">
                <TextBlock Text="⏱️" FontSize="48" HorizontalAlignment="Center"/>
                <TextBlock Text="No cron jobs configured"
                           Style="{StaticResource SubtitleTextBlockStyle}"
                           HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- Add Job Button -->
            <Button x:Name="AddJobButton" Content="+ Add Job" Click="OnAddJobClick"
                    HorizontalAlignment="Left"/>

        </StackPanel>
    </ScrollViewer>
</Page>
