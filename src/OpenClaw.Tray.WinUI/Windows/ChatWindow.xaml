<?xml version="1.0" encoding="utf-8"?>
<winex:WindowEx
    x:Class="OpenClawTray.Windows.ChatWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:winex="using:WinUIEx"
    xmlns:controls="using:OpenClawTray.Controls"
    Title="OpenClaw Chat">

    <Window.SystemBackdrop>
        <MicaBackdrop/>
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Thin popup header -->
        <Grid x:Name="ChatTitleBar" Grid.Row="0" Height="36" Padding="12,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                <TextBlock Text="🦞" FontSize="14"/>
                <TextBlock Text="Chat" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="2" VerticalAlignment="Center">
                <Button ToolTipService.ToolTip="Home" Click="OnHome"
                        Width="28" Height="28" Padding="0" Background="Transparent" BorderThickness="0">
                    <FontIcon Glyph="&#xE80F;" FontSize="12"/>
                </Button>
                <Button ToolTipService.ToolTip="Refresh" Click="OnRefresh"
                        Width="28" Height="28" Padding="0" Background="Transparent" BorderThickness="0">
                    <FontIcon Glyph="&#xE72C;" FontSize="12"/>
                </Button>
                <Button ToolTipService.ToolTip="Open in Browser" Click="OnPopout"
                        Width="28" Height="28" Padding="0" Background="Transparent" BorderThickness="0">
                    <FontIcon Glyph="&#xE8A7;" FontSize="12"/>
                </Button>
                <Button Click="OnCloseClick"
                        Width="28" Height="28" Padding="0" Background="Transparent" BorderThickness="0"
                        ToolTipService.ToolTip="Close">
                    <FontIcon Glyph="&#xE8BB;" FontSize="12"/>
                </Button>
            </StackPanel>
        </Grid>

        <!-- Shared chat surface (native ChatShell + WebView2) -->
        <controls:ChatSurface x:Name="Surface" Grid.Row="1"/>
    </Grid>
</winex:WindowEx>
